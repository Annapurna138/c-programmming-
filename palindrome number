#include<stdio.h>
main()
{
  int N;
  int s;
  int rem;
  int rev=0;

  printf(" entre N:");
  scanf("%d",&N);
  s=N;
  rev =0;
  while (N)
 {
   rem=N%10;
   rev=rev*10+rem;
   N=N/10;

 }
  if(s==rev)
   printf("palindrome number\n");
else
  printf("not palindrome number\n");


